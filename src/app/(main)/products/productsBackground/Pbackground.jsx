import style from "./Pbackground.module.css"

const ProductBackround = ({className}) =>{
    return(

        <div className={`${style.productBackround} ${className}`}>

        </div>
    )
}
export default ProductBackround;
