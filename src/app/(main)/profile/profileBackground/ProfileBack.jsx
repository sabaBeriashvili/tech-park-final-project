import style from './profileBack.module.css';

const ProfileBackround = ({className}) =>{
    return(
        <div className={`${style.ProfileBackround} ${className}`}>

        </div>
    )
}
export default ProfileBackround;




