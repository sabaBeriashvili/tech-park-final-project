import style from './backgound.module.css';

const CartBackround = ({className}) =>{
    return(
        <div className={`${style.cartBackround} ${className}`}>

        </div>
    )
}
export default CartBackround;




